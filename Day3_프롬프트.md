# Day 3 프롬프트 15개 — 내 도메인판

원본: `교안/8W Day3_교안.pdf` (30쪽) · 도메인: 경영계획 수립 퍼널 (590건, 2022~2026 합성)
작성 2026-09-03

## 오늘 확정한 값

| 항목 | 값 | 근거 |
|---|---|---|
| 못 믿을 조건 ① | 칸 표본 **30건** 미만 | 부서 15칸 29~48건 · 병목 구간 분모 27~45건 |
| 못 믿을 조건 ② | `cycle_year > 2025` (가드레일 한정) | 2026 확정배포 95건 중 `plan_actuals` **0건** |
| 못 믿을 조건 ③ | 비교 두 구간의 예산삭감연도(2023·2025) 포함 여부가 다름 | 통과율 삭감연도 69.49·68.50 vs 평년 76.07·80.18·81.20 |
| 주지표 움직임 | **6%p** | 조건 ③ 적용 후 평년끼리 변동 폭 5.13%p (σ 2.72) |
| 가드레일 악화 | **2%p** | 평년끼리 1.28%p · 삭감끼리 1.38%p |
| 성숙 기준 | **165일** (p90) | 제출→확정배포 중앙 126 · p90 165 · p95 176 |
| 판정 축 | **`department_name`** (보류 해제) | 병목 구간 격차 19.3%p, 손을 쓸 수 있는 유일한 축 |
| 구간 | 경영진검토 → 이사회승인 | 최대 병목 87.2% |

`division_type` 은 병목 구간 격차 0.2%p — **이 축으로는 안 갈린다.** 그것도 결과이므로 화면 후보로는 남긴다.

## config.py 에 더할 상수 (값은 여기에만)

```
MIN_CELL_SAMPLE      = 30          # 분해 칸 기준. MIN_SAMPLE(12, 주지표)과 다른 것
MIN_GUARDRAIL_SAMPLE = 15          # 부서×연도 조합당 (명세 4행, 지금까지 미구현)
BUDGET_CUT_YEARS     = {2023, 2025}
ACTUALS_LAST_YEAR    = 2025        # plan_actuals 가 존재하는 마지막 연도
MATURITY_DAYS        = 165         # 제출→확정배포 90분위
MOVE_THRESHOLD       = 6.0         # 주지표 %p
GUARDRAIL_MOVE       = 2.0         # 가드레일 %p
```

---

# 실습 A — 분해

## 프롬프트 1 — 분해 · 작업형

```
pages/2_대시보드.py 의 DIMS 에 "department_name" 을 더하고 기본 축으로 삼아줘.

DIMS = ["department_name", "division_type", "cycle_year"]

어느 구간을 볼 것인가: 경영진검토 → 이사회승인 (병목 87.2%)

core/metrics.py 의 funnel_by() 는 이미 구현돼 있다. docstring 의 반환 형식
(도달·전환·전환율·비중)을 그대로 지키고, 부서 축에서도 도는지 확인해줘.

결과를 표로 보여주고, 격차가 가장 큰 두 칸이 어디인지 알려줘.
각 칸의 비중(그 구간 도달 건수 중 몇 %)도 같이.

funnel_by() docstring 에 "세 번째(department_id)는 보류" 라고 적혀 있는데
오늘 그 보류를 풀었다. 그 줄도 같이 고쳐줘.
```

**확인할 것**
- 부서 15칸이 다 뜨는가 (도달 27~45건)
- 격차 두 칸이 인사팀 97.1% · IT팀 77.8% 로 나오는가
- 비중 합이 1 이 되는가 (안 되면 빠진 칸이 있다)

---

# 실습 B — 못 믿을 조건 ★ 오늘의 핵심

## 프롬프트 2 — 못 믿을 조건 분기 · 작업형

```
core/metrics.py 의 trust_check() 를 채워줘.

지금 시그니처는 (srm, n_total, days) 인데 내 도메인에는 A/B 실험이 없다.
배정 검사(srm) 인자를 빼고 아래 셋으로 바꿔줘. 호출부도 같이 고친다.

못 믿을 조건:
  1. 칸 표본이 30건 미만
  2. cycle_year 가 2025 보다 뒤 — 실적 대조 기간이 안 지났다 (가드레일에만 적용)
  3. 비교하는 두 구간의 예산삭감연도(2023·2025) 포함 여부가 다르다

하나라도 걸리면 사유 문자열을 돌려주고, 다 통과하면 None 을 돌려준다.

★ 사유에는 실제 숫자를 넣어줘.
  "표본 부족" 이 아니라 "표본 27건 (최소 30)" 처럼.
  2번은 "2026년 확정배포 95건 중 실적 대조 0건" 처럼.
  3번은 "2024(평년) vs 2025(예산삭감연도) — 제도가 다름" 처럼.

기준값 30 · 2025 · {2023, 2025} 는 core/config.py 에 상수로 두고 가져다 써.
metrics.py 에 숫자를 직접 쓰지 마.
MIN_SAMPLE(12) 은 주지표 기준이라 그대로 두고, 칸 기준은 새로 만든다.
funnel_by() 의 칸 필터도 MIN_SAMPLE 에서 그 새 상수로 바꿔줘.
```

## 프롬프트 3 — 실제로 감추는지 확인 · 즉답형

```
지금 데이터에서 trust_check() 에 걸리는 항목이 몇 개인지 보여줘.
부서 축 15칸과 가드레일(부서×연도) 60칸을 각각.
걸린 항목은 화면에 어떤 상태로 나오는지도.
```

**나와야 하는 값** — 이게 안 나오면 조건이 안 걸린 것이다
- 부서 축 병목 구간: **IT팀 27건 · 해외사업팀 28건** 두 칸이 감춰짐
- 가드레일 부서×연도: **60칸 전부** 15건 미만 (n 2~12) → 이 단위 보고 불가
- 2026 코호트 가드레일: 실적 0건 → 감춰짐

**대시보드를 열어 눈으로**
1. 걸린 항목에 사유가 뜬다
2. **걸린 항목에 지표 값이 안 보인다** ← 가장 중요
3. 색이 `block` 으로 갈린다

---

# 실습 C — 판정 카드

## 프롬프트 4 — 판정 규칙 · 작업형

```
지표가 계산된 항목에 판정을 붙여줘. 순서대로 확인한다.

1. 주지표(전체 통과율)가 6%p 이상 움직였는가 — 아니면 "효과 없음"
2. 가드레일(계획-실적 괴리율)이 2%p 이상 나빠졌는가 — 그러면 "주의 필요"
3. 둘 다 통과하면 "성공"

★ 2번을 1번 뒤에 두고, 1번을 통과했다고 바로 성공으로 가지 마.

가드레일을 계산할 수 없는 2026 코호트는 "성공" 으로 가지 말고
"가드레일 없음 — 무엇을 희생했는지 확인되지 않음" 으로 적어줘.

판정에 따라 카드 색이 갈리게 해줘.
색 정의는 config.COLORS 에서 가져다 쓰고 새로 만들지 마.
"계획-실적 괴리율" 은 높을수록 나쁜 지표다 — config.HIGHER_IS_WORSE 에 이미 있다.

6%p · 2%p 도 config.py 에 두고 가져다 써.

실험이 없는 도메인이라 카드는 연도 코호트 간 비교다.
"통계적으로 유의" 라는 표현은 쓰지 마. p값·신뢰구간도 계산하지 마.
```

**왜 6%p 인가** — 예산삭감연도를 섞으면 전년 대비 변화가 −6.58 / +10.69 / −11.68 / +12.70%p로 뛴다. 이건 성과가 아니라 격년 진동이다. 조건 ③으로 걸러낸 뒤 평년끼리 폭이 5.13%p라 6%p가 "평소를 넘었다"의 경계가 된다.

## 프롬프트 5 — 카드 확인 · 즉답형

```
지금 만들어진 카드들을 판정별로 몇 개씩인지 세어줘.
그리고 각 카드에 무엇이 표시되고 무엇이 표시되지 않는지 알려줘.
```

**확인할 것** — 색이 판정과 일치하는가 · 못 믿을 항목에 값이 없는가 · 강조 색이 제목·테두리 장식에 안 쓰였는가

---

# 실습 D — 시킨 대로 됐는지

## 프롬프트 6 — 무엇이 바뀌었는지 · 즉답형

```
어제 커밋 이후 바뀐 파일을 git diff --stat 으로 보여줘.
그리고 아래 둘을 알려줘.
- 내가 오늘 고치라고 한 파일은 core/metrics.py · core/config.py ·
  pages/2_대시보드.py 인데, 그 밖에 바뀐 것이 있는가
- 있으면 어느 파일의 어느 부분인지
되돌리지는 마. 목록만.
```

## 프롬프트 7 — 원칙이 코드에 들어갔는지 · 즉답형

```
trust_check() 와 그것을 부르는 쪽 코드를 읽고 아래를 판정해줘.

못 믿을 조건에 걸렸을 때
  (가) 지표를 계산한 뒤 화면에 안 그리는가
  (나) 지표를 계산조차 하지 않는가

(가)이면 어느 줄에서 계산이 일어나는지 알려줘.
고치지는 마. 판정만.
```

(가)로 나오면 → `판정을 계산 앞으로 옮겨줘. 못 믿을 조건에 걸리면 지표 계산 자체를 하지 않고 사유만 돌려주게.`

## 프롬프트 8 前 — CLAUDE.md 어긋난 곳 찾기

```
CLAUDE.md 에 적힌 내용과 지금 코드가 어긋난 곳을 찾아줘.
특히 "미정" 이라고 적힌 것 중 이미 정해진 것이 있는지.
고치지는 마. 목록만.
```

**오늘 풀리는 「미정」** — 분해축 세 번째(`department_id`) 보류 해제 · `funnel_by()` 인자 구성 · 가드레일 최소 표본 15 코드화 · 유효구간/성숙 기준

## 프롬프트 8 — CLAUDE.md 갱신 · 작업형

```
CLAUDE.md 에 오늘 정한 것을 덧붙여줘. 기존 내용은 지우지 마.

- 못 믿을 조건 셋과 각 기준값
    ① 칸 표본 30건 미만
    ② cycle_year > 2025 — 실적 대조 미도래 (가드레일 한정)
    ③ 비교 두 구간의 예산삭감연도 포함 여부가 다름
- 판정 순서: 못 믿을 조건 → 주지표(6%p) → 가드레일(2%p)
- 못 믿을 때는 계산하지 않는다 (계산 후 숨기는 것이 아니다)
- 감춘 항목에는 사유만 적고 수치는 적지 않는다
- 분해 축: department_name 으로 판정한다. division_type 은 병목 구간 격차가
  0.2%p 라 이 축으로는 갈리지 않는다 — 화면 후보로만 남긴다
- 성숙 기준 165일(p90). 주지표 유효구간은 2026 포함, 가드레일은 2025까지.
  창을 통일하지 않고 조건 ②로 표현한다

「아직 안 정한 것」에서 오늘 정해진 항목은 빼고 정해진 값으로 옮겨줘.
```

---

# 실습 E — 게이트 2

## 프롬프트 9 — 점검 자료 · 작업형

```
게이트 2에서 판단할 자료를 정리해줘.
- 주요 지표의 현재값과 상식 범위를 벗어나는 것이 있는지
- 감춰진(못 믿을) 항목의 개수와 사유
- 분해한 칸들의 합이 전체와 맞는지
판단은 하지 말고 사실만.
```

## 프롬프트 10 — 합계 검증 · 즉답형

```
분해한 각 칸의 계획안 수 합이 전체와 같은지 확인해줘.
- 부서 15칸의 합이 plans 590건과 같은가
- 병목 구간 도달 칸의 합이 경영진검토 도달 538건과 같은가
다르면 얼마나 다르고, 어디로 갔는지 알려줘.
funnel_by() 가 "(미상)" 으로 채우는 건이 몇 건인지도.
```

**차이가 나면** — 표본 30건 미만으로 빠진 칸(IT팀·해외사업팀)이 원인일 수 있다. 그건 오류가 아니라 오늘 만든 조건이 작동한 것이다. 다만 **몇 건인지 화면에 남기고** 금요일 한계 절에 적는다.

## 프롬프트 11 — 게이트 2 기록 확인 · 즉답형

```
게이트 2 통과 기록이 남았는지 확인해줘.
그리고 게이트 1과 2의 근거가 아카이브 화면에 나란히 보이는지도.
```

**게이트 2 근거 문구 (초안)**

> 주요 지표 2종 상식 범위 내(전체 통과율 74.92% · 괴리율 중앙 10.28%p). 부서 축 15칸 중 2칸(IT팀 27건 · 해외사업팀 28건)이 표본 30건 미만으로 판정 보류 — 다음 사이클 재확인 조건. 2026 코호트는 실적 대조 기간이 지나지 않아(확정배포 95건 중 실적 0건) 가드레일을 판정하지 않음. 주지표는 성숙 기준 165일(p90)을 충족하므로 2026을 포함해 통과. 성과 문제가 아니라 관측 기간 문제다.

---

# 실습 F — 판정 과정 노출

## 프롬프트 12 — st.status · 작업형

```
판정 카드에 판정 과정을 st.status 로 보여주는 부분을 추가해줘.

- 카드마다 with st.status("판정 과정", expanded=False) as box: 로 하나 둔다
- 그 안에 순서대로 st.write 로 남긴다
  1) 못 믿을 조건 확인 — 통과 / 걸림(사유)
  2) 주지표(전체 통과율) — 값 (걸렸으면 "계산하지 않음")
  3) 가드레일(계획-실적 괴리율) — 값과 판정 (걸렸으면 "계산하지 않음")
- 마지막에 box.update(label=<최종 판정>, state=...) 로 반드시 닫는다
    무효  state="error"  /  그 밖  state="complete"

★ st.status 를 다시 호출해서 update 하지 마. 위젯이 하나 더 생긴다.
  with 이 돌려준 box 를 잡아서 그것만 update 해야 한다.
★ 안 닫으면 state 가 "running" 으로 남아 스피너가 계속 돈다.

못 믿을 조건에 걸린 경우 2)와 3)에 값을 넣지 마. "계산하지 않음" 만 적는다.
```

---

# 실습 G — 감춘 이유 모달 ★ 오늘의 장면

## 프롬프트 13 — 근거 모달 · 작업형

```
감춰진 카드에 "왜 감췄나" 버튼을 만들고, 누르면 모달로 근거를 보여줘.

1. @st.dialog("이 값을 왜 보여주지 않나") 로 함수를 하나 만든다
2. 그 안에 표로 보여준다
   - 걸린 조건 (셋 중 무엇)
   - 실제 값 (표본 수 · cycle_year · 비교 구간의 예산삭감연도 여부만)
   - 무엇을 하면 믿을 수 있는가 (한 줄)
3. 카드의 버튼에서 그 함수를 호출한다

"무엇을 하면 믿을 수 있는가" 예시:
  표본 부족   → "부서를 사업부/지원부서로 묶으면 235·355건이 되어 판정 가능"
  대조 미도래 → "2026년 실적이 적재되면(대조 기간 경과 후) 판정 가능"
  구간 불공정 → "같은 성격의 연도끼리(평년 2022·2024·2026) 비교하면 판정 가능"

그리고 지표 이름 옆에 st.popover("정의") 를 붙여서
계산식과 임계값 근거를 짧게 보여줘.
  전체 통과율 = final_status='확정배포' / plans 전체 × 100 · 임계값은 임의지정
  계획-실적 괴리율 = plan_actuals.variance_pct
                    경고 10.28(347건 중앙값) · 위험 16.76(예산삭감연도 평균 n=169)

모달 안에 지표 값·증감·p값을 넣지 마. 조건 값만이다.
```

---

# 실습 H — URL 상태

## 프롬프트 14 — URL 에 상태 싣기 · 작업형

```
대시보드의 분해 축과 기간 필터를 둘 다 st.query_params 와 연결해줘.

1. 분해 축 선택을 st.radio 에서 st.segmented_control 로 바꾼다
   (pages/2_대시보드.py 71~72줄. 후보는 department_name · division_type · cycle_year,
    기본은 department_name)
2. 페이지가 뜰 때 st.query_params 에서 값을 읽어 초기값으로 쓴다
3. 사용자가 바꾸면 st.query_params 를 갱신한다
4. 화면 어딘가에 "현재 화면 링크" 를 st.code 로 보여줘서 복사할 수 있게 한다

없는 파라미터가 오거나 값이 이상하면 기본값으로 떨어지게 해줘. 에러를 내면 안 된다.

⚠ 어제 만든 st.fragment 안에서 st.query_params 를 갱신하면 조각만 다시 그려져
  URL과 화면이 어긋난다. 필터는 fragment 밖에 두십시오.
```

**확인할 것** — `?dim=없는축` 으로 넣어도 안 죽는가 · 새 탭에 붙이면 같은 화면인가

---

# 마무리

## 프롬프트 15 — 판단 기준 한 장 · 작업형

```
오늘 배운 것 중 도메인과 무관한 문장만 남겨 한 장으로 정리해줘.
- 특정 업종·데이터 이름이 남아 있으면 빼줘
  (경영계획·부서·예산삭감연도·확정배포 같은 단어가 남으면 안 된다)
- 내가 오늘 내린 결정도 한 줄씩
  · 무엇으로 쪼갰고 왜 — 격차가 큰 축이 아니라 손을 쓸 수 있는 축을 골랐다
  · 어떤 조건이면 못 믿는다고 정했고 왜 — 표본·대조 기간·비교 공정성 셋
  · 왜 판정 창을 지표마다 다르게 두었는가
- 판단기준.md 에 날짜와 함께 덧붙여줘. 덮어쓰지 마
```

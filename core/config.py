# -*- coding: utf-8 -*-
"""앱 설정.

**도메인을 바꿀 때 가장 먼저 고치는 곳이 이 파일이다.**

다만 이 파일만 고치면 끝나지 않는다. 컬럼명은 metrics.py 안에도 박혀 있어서,
config를 다 바꾸고 앱을 켜면 거기서 깨진다. **깨지는 것이 정상이다.**
순서는 DESIGN.md §4-6에 있다.

    1. 여기 FUNNEL_STEPS 를 내 단계로 바꾼다
    2. load.py 가 내 파일을 읽게 한다
    3. 앱을 켠다 -> 깨진다
    4. metrics.py 의 컬럼명을 하나씩 맞춘다
    5. validate.py 에서 내 데이터에 맞지 않는 검증을 뺀다

────────────────────────────────────────────────────────────────────
7주차 토요일에 만든 「명세 5행」이 여기로 들어온다.

  명세 1행  데이터                 ->  DATASET · PERIOD · TABLES
  명세 2행  퍼널 단계              ->  FUNNEL_STEPS · FUNNEL_LABELS
  명세 3행  주지표 + 가드레일       ->  metrics.py 에서 계산
  명세 4행  임계값 · 최소 표본      ->  THRESHOLDS · MIN_SAMPLE
  명세 5행  유지 / 이탈의 정의      ->  RETENTION_STEPS
────────────────────────────────────────────────────────────────────

내 도메인: **경영계획(사업계획) 수립 퍼널** — 최종 제안 대상은 상위 경영진(계획 확정권자).
명세 원본은 project3-wiki/나의_성장퍼널.md 상단 표, 실측 근거는 mydomain/notes/ 3개 노트다.

  01_데이터.md   테이블 그레인 · 기간 · 결측
  02_퍼널실측.md  5단계 도달 수 · 전환율 · "진짜 퍼널" 판정
  03_지표정의.md  주지표(전체 통과율) 정의서 · 임계값 근거

비즈니스 파라미터(CHANNEL_CAC 등)만 통신사 값이 남아 있다 — 쓰이는 곳이
선택 과제인 metrics.channel_efficiency() 뿐이라 그대로 두었다.
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
DATA_DIR = ROOT / "data"          # 데이터 파일
RUNS_DIR = ROOT / "runs"          # 실행 이력
ASSETS_DIR = ROOT / "assets"      # 폰트 등

# ── 데이터 ────────────────────────────────────────────────────────
# ★ Day1 — 명세 1행
#   (c) 명세 합성 데이터다. 실무 시스템에 해당 데이터가 없어(2026-08-29 확인)
#   generate_plan_funnel_data.py 로 시드 고정 생성했다. 결측 1.17% · 중복 64건은
#   버그가 아니라 의도적으로 심은 것이다.
DATASET = "경영계획 수립 퍼널 (2022~2026, 합성)"
#   두 테이블이 모두 들어가는 범위로 잡았다. 벗어나면 날짜 범위 검증에 걸린다.
#     plans.submitted_date          2022-01-01 ~ 2026-03-16
#     plan_stage_events.event_date  2022-01-02 ~ 2026-08-29
#   주지표의 유효구간은 이보다 좁다 — 연도별 1~3월만이고 4~12월은 제출 0건이다.
#   (값이 0%인 것이 아니라 표본이 없다. -> 03_지표정의.md)
PERIOD = ("2022-01-01", "2026-08-31")

# ★ 내 테이블 목록. 하나뿐이어도 된다. 파일명(확장자 제외)과 같아야 한다.
TABLES = [
    "departments",        # 부서 마스터
    "plans",              # 과제(계획) 마스터 — 퍼널의 대상
    "plan_stage_events",  # 단계별 심의 이벤트 — 퍼널 계산의 원천
    "plan_actuals",       # 계획 대비 실집행 — 확정된 과제만 존재
]
# tmp_dept · tmp_divtype · tmp_year 는 집계 결과(파생)라서 넣지 않았다.
# generate_plan_funnel_data.py 는 데이터가 아니라 생성 스크립트다.

# ── 퍼널 ──────────────────────────────────────────────────────────
# ★ Day1 — 명세 2행. 순서가 곧 단계 순서다.
#   단계 수는 도메인이 정한다. 6개일 이유가 없다 — 3개여도 8개여도 된다.
#   퍼널이 아니면 퍼널이라고 부르지 않는다: 앞 단계를 반드시 거치는가?
#
#   여기 값은 plan_stage_events.stage_name 의 **실제 값과 글자까지 같아야 한다.**
#   계산이 이 문자열로 필터하기 때문에 띄어쓰기를 넣으면 전부 0으로 나온다.
#   화면에 보이는 띄어쓴 이름은 아래 FUNNEL_LABELS 에서만 쓴다.
#
#   stage_seq 1~5 와 1:1 대응이고, 그레인은 **계획안(부서×연도×과제) 1건**이다.
#   "진짜 퍼널인가" 검사 통과 — 앞 단계를 거치지 않고 다음 단계에 도달한 건이
#   0건이고 비연속 경로도 0건이다. (-> 02_퍼널실측.md)
#
#   도달 수  590 -> 558 -> 538 -> 469 -> 442   (누적 전환율 74.9%)
#   전환율        94.6%  96.4%  87.2%  94.2%
#                               ^^^^^ 최대 병목: 경영진 검토 -> 이사회 승인
FUNNEL_STEPS = ["부서초안제출", "예산실1차조정", "경영진검토", "이사회승인", "확정배포"]
FUNNEL_LABELS = {
    "부서초안제출": "부서 초안 제출",
    "예산실1차조정": "예산실 조정",   # 차트 라벨이 줄바꿈되지 않게 줄였다
    "경영진검토": "경영진 검토",
    "이사회승인": "이사회 승인",
    "확정배포": "확정 배포",
}

# ── 유지 퍼널 ─────────────────────────────────────────────────────
# ★ Day2 — 명세 5행. 7주차 토요일에 내가 정한 유지·이탈의 정의를 옮긴다.
#
#   획득 퍼널은 단계가 주어졌지만 유지는 순서가 없다. 내가 정의해야 한다.
#   각 단계는 (이름, 설명) 이고, 계산은 metrics.retention_funnel() 에서 한다.
#
#   예시 - 통신사:
#     [("가입", "전체 고객"),
#      ("활성", "마지막 달에 사용 기록이 있음"),
#      ("이탈", "is_churned = True")]
#
#   이탈 개념이 없는 도메인이면 재이용률·반복 요청률로 바꾼다. (DESIGN.md §4-4)
#
#   내 정의(명세 5행): **철회 + 반려종결 = 이탈.** 그 외는 유지.
#   철회·반려종결은 되돌릴 수 없는 확정 종료이고, 확정배포대기(3건)는 아직 결론이
#   안 난 진행 중 상태라 이탈로 볼 수 없다.
#
#   주의 — 주지표(전체 통과율)와 분자가 다르다. 통과율은 확정배포만 세서 74.92%,
#   유지율은 확정배포대기까지 세서 75.42%다. 3건 차이다.
#
#   3단계로 정했다. 뒤로 갈수록 조건이 좁아지는 **부분집합** 관계다.
#       제출 590  ⊃  유지 445  ⊃  확정 배포 442
#
#   마지막 구간은 445 -> 442 (99.3%) 로 3건만 걸러진다. 그 3건이 미결
#   (확정배포대기)이다 — 유지와 확정 배포를 가르는 것이 미결뿐이기 때문이다.
#
#   **이탈(145건)은 단계에서 뺐다.** 유지(445)의 여집합이라 앞 단계를 거친
#   부분집합이 아니고, 단계로 넣으면 445 -> 145 -> 189 로 숫자가 다시 늘어
#   전환율 해석이 안 된다. 이탈은 유지의 나머지(590 - 445)로 읽는다.
#   명세 5행의 이탈 정의 자체는 그대로 유효하다 — 퍼널 단계로 쓰지 않을 뿐이다.
RETENTION_STEPS: list[tuple[str, str]] = [
    ("제출", "전체 계획안 — plans 전체 행 (590건)"),
    ("유지", "final_status 가 확정배포 또는 확정배포대기 (445건, 75.42%)"),
    ("확정배포", "final_status 가 확정배포 (442건, 74.92%)"),
]

# ── 비즈니스 파라미터 ─────────────────────────────────────────────
# 전부 **가정값**이다. 실측 테이블에서 유도하지 않는다.
# 광고비(ad_spend)는 채널 효율(ROAS·CPC) 전용이고, 총액을 가입자 수로 나눠
# CAC를 만들지 않는다 — 광고비는 개인 단위로 추적되지 않아 가입과 이을 수 없다.
# 리포트에 이 값이 들어가는 문장에는 "가정값 기반"을 붙인다. -> DESIGN.md §4-3
#
# ★ 비용 개념이 없는 도메인이면 투입 공수(인시)로 바꿔도 된다. 금액이 아니어도
#   "무엇을 얼마나 썼는가"면 된다.
CHANNEL_CAC = {
    "검색": 140_000, "디스플레이": 180_000, "제휴": 100_000,
    "추천": 30_000, "오프라인": 250_000,
}
CONTRIBUTION_MARGIN = 0.30      # 공헌이익률
LTV_MONTHS = 36                 # 평균 유지기간

# ── 지표 임계값 ───────────────────────────────────────────────────
# ★ Day1 — 명세 4행. **비워 두었다. 내가 정한다.**
#
#   남이 정해줄 수 없는 유일한 값이다. 업계 벤치마크·직전 기간·목표치 중
#   무엇을 근거로 삼았는지 위키 지표 정의서에 한 줄로 적는다.
#   근거 없는 임계값은 남의 것이다.
#
#   지금은 비어 있어서 지표 카드가 전부 "정상"으로 뜬다. 채우면 색이 갈린다.
#
#   형식:  "지표이름": {"경고": 값, "위험": 값}
#   예시 - 통신사:
#     "전환율": {"경고": 3.0, "위험": 2.5},      # % 미만이면
#     "이탈률": {"경고": 18.0, "위험": 22.0},    # % 초과면
#
#   ⚠ 여기 키는 metrics.kpis() 가 돌려주는 **지표 이름과 정확히 같아야** 색이 갈린다.
#     Day2 에 kpis() 를 채울 때 이름을 "전체 통과율" · "계획-실적 괴리율" 로 쓴다.
#   ⚠ "계획-실적 괴리율" 은 **높을수록 나쁜** 지표다. metrics.status_of() 의
#     higher_is_worse 목록에 넣어 두었다 — 안 넣으면 판정 방향이 거꾸로 뒤집힌다.
THRESHOLDS: dict[str, dict[str, float]] = {
    # 주지표 — 부서 초안 제출 -> 확정 배포. % **미만**이면 걸린다. 현재값 74.92%
    #   근거: 임의지정. 사용자가 직접 정한 값이고 통계적 산출 근거는 없다.
    #   (근거가 없다는 사실 자체를 적어 둔다. -> 03_지표정의.md)
    "전체 통과율": {"경고": 70.0, "위험": 65.0},

    # 가드레일 — 통과율을 무리하게 올리면(검토를 형식적으로 넘기면) 확정된 계획의
    #   정확도가 떨어질 수 있다. %p **초과**면 걸린다.
    #   근거: plan_actuals 347건 실측 (2026-08-29 직접 조회)
    #     경고 10.28 = 전체 중앙값
    #     위험 16.76 = 예산삭감연도(2023·2025) 평균, n=169
    #   함께 본 값: 상위 25% 19.73 · 평균+1σ 24.12
    "계획-실적 괴리율": {"경고": 10.28, "위험": 16.76},
}

# 높을수록 나쁜 지표. 이름을 여기 넣지 않으면 판정 방향이 뒤집혀,
# 좋은 값(괴리율이 낮음)이 위험으로 뜬다. metrics.status_of() 가 읽는다.
# THRESHOLDS 와 짝이므로 바로 옆에 둔다 — 임계값을 더할 때 방향도 같이 정한다.
HIGHER_IS_WORSE = {"계획-실적 괴리율", "이탈률", "이탈율", "해지율", "불량률", "반품률"}

# ── 못 믿을 조건 (metrics.trust_check) ────────────────────────────
# MIN_SAMPLE 과 **다른 것**이다. MIN_SAMPLE(12) 은 주지표 전체의 최소 표본이고,
# 이쪽은 분해해서 만든 **칸 하나**의 최소 표본이다. 칸은 전체보다 작으므로
# 기준이 따로 있어야 한다.
#
#   30 의 근거: 분해 축 중 칸이 가장 얇은 것이 부서(15칸)이고, 병목 구간에서
#   칸당 도달이 27~45건이다. 30 을 넘지 않는 칸은 비율로 말하지 않는다.
MIN_CELL_SAMPLE = 30

# 실적 대조(plan_actuals)가 존재하는 마지막 cycle_year.
# 이보다 뒤의 연도는 실적 대조 기간이 아직 안 지나 가드레일을 계산할 수 없다.
# **결측이 아니라 의도된 설계다.** 데이터가 갱신되면 이 값을 올린다.
ACTUALS_LAST_YEAR = 2025

# 예산삭감연도. 이 해와 평년은 제도가 달라 같은 기준으로 비교할 수 없다.
# 두 구간을 비교할 때 한쪽만 이 집합에 걸리면 차이를 개입 효과로 읽을 수 없다.
BUDGET_CUT_YEARS = {2023, 2025}

# 검증 규칙 1(행 수) 의 경고 기준. 적재된 행 수가 기준 대비 이 비율을 벗어나면
# 경고. 차단이 아닌 이유는 데이터가 갱신되면 늘어나는 것이 정상이기 때문이다.
ROW_TOLERANCE = 0.10

# ★ 이보다 표본이 적으면 "표본 부족"으로 보고 지표를 계산하지 않는다.
#   내 상황에 맞게 낮춰도 된다. **다만 없애지는 않는다.** -> DESIGN.md §4-5
#
#   12 는 **주지표** 기준이다(명세 4행). 통신사 기본값 1,000 은 방문자 단위라
#   계획안 590건 도메인에 그대로 쓰면 전부 표본 부족으로 막힌다.
#   월별 코호트의 최소 표본은 17건(2024-03)이라 12 를 넘는다.
#
#   ⚠ 가드레일의 최소 표본은 성격이 달라 이 상수로 표현되지 않는다 —
#     **부서·연도 조합당 15건 이상**일 때만 보고한다(명세 4행).
#     부서별로는 n 15~31 로 전 부서가 대상이지만, 부서×연도로 쪼개면 n 이 2까지
#     떨어져 이 단위 보고는 명세 위반이다. Day3 분해·가드레일 계산에서 막는다.
#     (-> 01_데이터.md 4절)
MIN_SAMPLE = 12

# ── 상태 색 ───────────────────────────────────────────────────────
# 의미가 고정된 규칙이다. **도메인이 바뀌어도 바꾸지 않는다.**
COLORS = {
    "ok": "#10b981",       # 통과 / 정상
    "warn": "#f59e0b",     # 경고 / 표본 부족 / 가드레일 악화
    "block": "#f43f5e",    # 차단 / 검증 실패 / 실험 무효
    "none": "#64748b",     # 데이터 없음 / 효과 없음 / **판정 기준 없음**
}
# 브랜드 색은 자유롭게 바꿔도 된다.
BRAND = {
    "primary": "#4f46e5",
    "ink": "#0f172a",
    "muted": "#64748b",
    "line": "#e2e8f0",
    "bg": "#f8fafc",
    "surface": "#ffffff",
}

# ── 리포트 ────────────────────────────────────────────────────────
# 8장 중 3장은 사람이 쓴다. **이 구분은 바꾸지 않는다.**
# 해석과 제안을 자동화하는 순간 책임이 사라진다.
REPORT_SECTIONS = [
    ("1. 요약", "auto"),
    ("2. 배경", "human"),
    ("3. 방법", "auto"),
    ("4. 결과", "auto"),
    ("5. 실험", "auto"),
    ("6. 해석", "human"),
    ("7. 한계", "auto"),
    ("8. 제안", "human"),
]

# 발송은 초안까지만. 실제로 전송하지 않는다.
EMAIL_ENABLED = False
EMAIL_TO_EXAMPLE = "team-growth@example.com"

# CLAUDE.md

**이 앱은 경영계획 수립 퍼널을 검증 → 계산 → 대시보드 → 리포트로 잇고, 사람은 게이트 세 곳에서만 판단하는 앱이다.**

이 문서에는 **이미 정한 것만** 적는다. 안 정한 것은 `미정`으로 남긴다 — 지어내서 채우지 않는다.

- 데이터셋 `경영계획 수립 퍼널 (2022~2026, 합성)` · 기간 `2022-01-01 ~ 2026-08-31`
- 원천 4테이블 `departments` · `plans` · `plan_stage_events` · `plan_actuals`
- 명세 원본 `project3-wiki/나의_성장퍼널.md` 상단 표 · 실측 근거 `mydomain/notes/` 01~03
- 회차별 판단 기록 `판단기준.md`

---

## 그레인

**계획안 1건.** `plan_id` 로 고유하게 센다.

명세의 정식 표현은 「계획안(부서×연도×과제) 1건」이다. `plans.plan_id` 590개가 행 수와 같아 PK다.
`plan_stage_events` 는 그레인이 다르다 — 「계획안이 특정 단계에서 겪은 시도 1회」이고 PK가 아니다
(`event_id` 고유 3,178 / 행 3,242, 완전중복 64행).

용어: 노트와 `config.py` 는 「계획안」, 대화에서는 「과제」로도 부른다. 같은 것을 가리킨다.

---

## 퍼널 단계와 도달 조건

`config.FUNNEL_STEPS` 순서가 곧 단계 순서다. 값은 `plan_stage_events.stage_name` 과 글자까지 같아야 한다.

| # | 단계 (데이터 값) | 화면 표시 | 도달 |
|---|---|---|---:|
| 1 | `부서초안제출` | 부서 초안 제출 | 590 |
| 2 | `예산실1차조정` | 예산실 조정 | 558 |
| 3 | `경영진검토` | 경영진 검토 | 538 |
| 4 | `이사회승인` | 이사회 승인 | 469 |
| 5 | `확정배포` | 확정 배포 | 442 |

**도달 조건 (전 단계 공통)** — 그 단계에서 `result = "통과"` 인 기록이 있는 계획안.
세기 전에 완전중복 64행을 제거하고, `result` 가 `반려`(523) · `철회`(74) 인 행은 도달로 세지 않는다.

- 스킵 0건 · 비연속 경로 0건으로 **진짜 퍼널로 판정됨** (합성 데이터 기준. 실측 데이터를 붙이면 재확인 필요)
- 최대 병목은 3→4 구간(경영진 검토 → 이사회 승인) 87.2%
- **단계별 개별 통과 조건(무엇을 충족하면 그 단계를 통과하는가)은 데이터에 없다 — `미정`**
  승인자 개인 식별자도 없다. 있는 것은 단계·날짜·결과·시도 회차뿐이다.

---

## 지표

### 주지표 — 전체 통과율 (`plan_overall_pass_rate`)

```
분자  final_status = '확정배포' 인 계획안 수
분모  plans 전체 행 수 (= 퍼널 1단계 도달 수)
산식  분자 / 분모 × 100
```

- 현재값 **74.92%** (442 / 590)
- 코호트 단위는 **제출월** (`submitted_date` 기준). 이벤트 발생월이 아니다
- 미결(`확정배포대기`, 3건)은 **분모에 포함, 분자에 미포함** — 확정 안 된 것을 통과로 세지 않는 보수적 기준
- 유효구간은 **연도별 1~3월만.** 4~12월은 제출 0건이라 값이 0%인 것이 아니라 표본이 없다
- 소유자 hyeji5721@gmail.com · 갱신주기 `미정`

### 퍼널 전환율 (`metrics.funnel()` — 구현됨)

```
n          그 단계를 통과한 plan_id 고유 수
step_rate  n / 순서상 직전 단계 n        (1단계는 결측 — 전 단계가 없다)
cum_rate   n / 1단계 n
drop       직전 단계 n - n               (1단계는 결측, dtype Int64)
is_bottleneck  step_rate 가 가장 낮은 구간
```

첫 단계의 `step_rate` 와 `drop` 은 둘 다 **결측**이다. 0 이 아니다 — 0 은 "빠진 것이 없다"로 읽힌다.
`config.FUNNEL_STEPS` 값이 데이터에 없으면 **`ValueError` 를 올린다.** 0 으로 두면 "아무도 통과 못 했다"와
"이름이 틀렸다"가 구분되지 않는다.

### 유지 / 이탈 (`config.RETENTION_STEPS`)

```
제출  plans 전체 행                                  590건
유지  final_status 가 확정배포 또는 확정배포대기      445건 (75.42%)
이탈  final_status 가 철회 또는 반려종결              145건 (24.58%)
```

철회·반려종결은 되돌릴 수 없는 확정 종료이고, 확정배포대기는 아직 결론이 안 난 진행 중 상태라 이탈로 보지 않는다.
주지표와 분자가 다르다 — 통과율은 확정배포만(74.92%), 유지율은 확정배포대기까지(75.42%). 3건 차이다.
계산 함수 `metrics.retention_funnel()` 은 아직 비어 있다.

### 가드레일 — 계획-실적 괴리율

`plan_actuals.variance_pct` 를 쓴다. **집계 방법(평균 / 중앙값 / 분포 어느 지점)은 `미정`.**
임계값만 정해져 있고 계산식 정의서는 아직 없다.

- 커버리지 주의: `plan_actuals` 는 확정배포 442건 중 **347건만**. 나머지 95건은 전부 2026년으로,
  실적 대조 기간이 아직 안 지나 존재하지 않는다(결측이 아니라 의도된 설계)

### 지표 카드 목록 (`metrics.kpis()`)

**`미정`.** 어느 지표를 카드로 올릴지 정하지 않았다.
정할 때 주의: `config.THRESHOLDS` 의 키와 카드 이름이 **글자까지 같아야** 색이 갈린다.

### 분해축

명세에 두 축이 정해져 있다 — `division_type`(사업부 9 / 지원부서 6) · `cycle_year`(특히 예산삭감연도 2023·2025 여부).
세 번째 축 `department_id` 는 보류. **`funnel_by()` 구현과 함수 인자 구성은 `미정`.**

### 실험

이 도메인에 A/B 실험(무작위 배정)은 **없다.** 전후 비교로 대체하며, 그 경우 인과 주장은 불가하다.

---

## 임계값과 근거

| 대상 | 값 | 방향 | 근거 |
|---|---|---|---|
| 전체 통과율 | 경고 70.0 · 위험 65.0 | % **미만** | **임의 지정** — 사용자가 직접 정한 값이고 통계적 산출 근거 없음 |
| 계획-실적 괴리율 | 경고 10.28 · 위험 16.76 | %p **초과** | 실측 — 경고는 `plan_actuals` 347건 전체 중앙값, 위험은 예산삭감연도(2023·2025) 평균 n=169 |
| 최소 표본 (`MIN_SAMPLE`) | 12 | 미만이면 계산 안 함 | 명세 4행 주지표 최소 표본. 월별 코호트 최소는 17건(2024-03)으로 충족 |
| 가드레일 최소 표본 | 부서×연도 조합당 15건 | 미만이면 보고 안 함 | 명세 4행. 부서별은 n 15~31로 충족하지만 부서×연도로 쪼개면 n 2까지 떨어진다. **코드 미구현** |
| 행 수 검증 (`ROW_TOLERANCE`) | 기준 대비 ±10% | 벗어나면 경고 | 갱신되면 늘어나는 것이 정상이라 차단할 근거가 없다 |

**높을수록 나쁜 지표**는 `config.HIGHER_IS_WORSE` 에 등록해야 한다.
현재 `계획-실적 괴리율` 이 들어가 있다. 빠지면 좋은 값(괴리율 5%)이 위험으로 뜬다.
`THRESHOLDS` 바로 옆에 두었다 — 임계값을 더할 때 방향도 같이 정하기 때문이다.

### 검증 규칙 4건 (`validate.run_checks()`)

| 규칙 | 기준 | 레벨 | 근거 |
|---|---|---|---|
| 행 수 | `MIN_SAMPLE` 미만 | **차단** | 계산하지 않기로 미리 정한 구간. 계산해도 쓸 수 없다 |
| 행 수 | 기준 대비 ±10% | 경고 | 갱신되면 늘어나는 것이 정상 |
| 필수 컬럼 | 4테이블 17개 컬럼 | **차단** | 없으면 계산이 멈추거나 지표가 조용히 빠진다 |
| 날짜 범위 | `PERIOD` 밖 | **차단** | 표지에 적은 기간과 집계 대상이 달라진다 |
| 단계 이름 | `FUNNEL_STEPS` 가 데이터에 없음 | **차단** | 그 단계가 0이 되어 틀린 결과가 정상처럼 보인다 |

날짜 결측 38행은 차단도 경고도 걸지 않고 **검증 상세에 건수를 노출**하고 리포트 7장 한계로 넘긴다 —
대체할 근거가 없으므로 감추지도, 차단하지도 않는다.

---

## 코드를 고칠 때 지킬 것

- **지표의 «값»은 `core/config.py` 에서만 바꾼다.** 임계값·판정 방향·단계 이름과 순서·기간·최소
  표본이 여기 해당한다. 다른 파일에 숫자나 상태값을 직접 쓰지 않는다.
- **지표의 «계산 방법»은 계산 파일에 둔다.** 판정식·필터·집계 방식은 `metrics.py` 다.
  값과 방법이 짝을 이루는 것(단계 이름 ↔ 판정식)은 **어긋나면 예외가 나야 한다** —
  조용히 빠지면 단계 하나가 사라진 채로 결과가 그려진다.

  | | 어디에 | 예 |
  |---|---|---|
  | 값 | `config.py` | `THRESHOLDS` · `HIGHER_IS_WORSE` · `MIN_SAMPLE` · `ROW_TOLERANCE` · `FUNNEL_STEPS` · `RETENTION_STEPS` |
  | 방법 | `metrics.py` | `_RETENTION_RULES` 판정식 · `result == "통과"` 필터 · `nunique` 로 세는 것 |
  | 검증 재료 | `validate.py` | `BASELINE_ROWS` · `REQUIRED_COLS` · `STEP_SOURCE` · `DATE_COLS` — 지표 정의가 아니라 규칙의 입력이다 |
- **계산 경로에 현재 시각을 넣지 않는다.** 넣으면 같은 입력이 같은 결과를 내지 않아 재현할 수 없다. 시각은 실행 기록(`run`)에만 남긴다.
- **시키지 않은 파일은 고치지 않는다.** 고칠 필요를 발견하면 먼저 말하고, 지시받은 범위만 바꾼다.

### 알아 둘 것

- `config.py` 를 고친 뒤에는 **캐시를 비워야** 반영된다. `@st.cache_data` 의 키는 인자뿐이라 설정 변경으로는
  무효화되지 않는다. 코드가 다시 실행되는 것과 캐시가 비워지는 것은 별개다.
- 통신사 골격의 테이블명이 아직 `pages/2_대시보드.py` · `3_리포트.py` · `4_아카이브.py` 에 남아 있다
  (`t["funnel_events"]` · `t["sessions"]`). 그 화면들은 지금 `KeyError` 로 죽는다.

---

## 아직 안 정한 것 (`미정` 모음)

- 지표 카드 목록 (`kpis()`)
- 가드레일 계산식 — `variance_pct` 를 어떻게 집계할지
- `funnel_by()` 구현 및 인자 구성
- `retention_funnel()` · `monthly()` 구현
- 단계별 개별 통과 조건 (데이터에 없음)
- 지표 갱신 주기
- 퍼널에서 **철회(74건)를 반려와 같이 볼지, 중도절단으로 분모에서 뺄지**
- `cum_rate` 분모를 이벤트 로그 1단계 도달 수로 둘지, `plans` 전체 행으로 둘지 (지금은 둘이 같은 590)
- 게이트 1 통과 직후 실행 기록을 파일로 저장할지 (현재는 게이트 2까지 가야 저장됨)
- 게이트 통과 시 판단 근거를 필수로 만들지 (현재는 비워도 통과됨)
- 리포트 자동 5장의 문장 구성
- 캐시 키에 config 를 넣을지 (현재는 수동으로 캐시를 비워야 함)

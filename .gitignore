__pycache__/
*.pyc
runs/*.json
.streamlit/secrets.toml

# data/ 는 커밋에 포함한다 — 합성 데이터(252KB)라 공개 정보가 아니고,
# 받은 사람이 바로 앱을 띄울 수 있다.
# **진짜 데이터로 바꾸면 여기에 data/* 를 다시 넣어야 한다.**
#   (data/ 가 아니라 data/* 로 써야 아래 형태의 예외가 동작한다)

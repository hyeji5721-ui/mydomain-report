# Day 5 프롬프트 12개 — 내 도메인판

원본: `교안/9W Day1_교안.pdf` (17쪽) · 도메인: 경영계획 수립 퍼널 (590건, 2022~2026 합성)
작성 2026-09-08 · 각 항목은 **원본 프롬프트(교안 그대로) → 치환 프롬프트(내 도메인)** 순으로 붙인다.

이번 회차는 Day3·Day4(코드를 고치는 회차)와 다르다 — **코드를 안 고치고 앱을 조회만 해서 `발견.md`를 만드는 회차**다. `report/sections.py`를 건드리지 않는다.

## 오늘 채우는 자리

```
발견.md          ← 실습 F · 프롬프트 11   (프로젝트 루트, my-report/ 아님 — 판단기준.md와 같은 자리)
판단기준.md       ← 판단 기준 한 장 · 프롬프트 12  (기존 파일에 날짜 덧붙임, 덮어쓰지 않음)
```

`my-report/` 폴더는 이 프로젝트에 없다. 교안의 `my-report/발견.md`·`my-report/판단기준.md`는 전부 루트 경로로 바꾼다.

## 오늘 실제로 확인한 값 (프롬프트에 넣을 재료)

```
metrics.kpis()        전체 통과율 74.92%(442/590) · 계획-실적 괴리율 13.30%p(평균, n=347) · 유지율 75.42%(445/590)
                       카드 3개뿐 — 넷째 칸은 비워 둔다(재도전율은 임계값 없어 뺐다)
metrics.funnel()       590 → 558 → 538 → 469 → 442. 최대 병목은 3→4(경영진검토→이사회승인) 87.2%
분해축 3개             department_name(판정축, 개입 가능) · division_type(후보만 — 병목 구간 격차 0.2%p, 실제로 안 갈림)
                       · cycle_year(예산삭감연도 2023·2025 여부, 정책 개입 가능하나 부서만큼 직접적이지 않음)
reviewer_pass_rate     이사회승인 위원별 53.4~72.3%(약 19%p) — 부서 축 격차와 비슷한 크기
못 믿을 조건 (trust_check) ① 칸 표본 30건 미만(MIN_CELL_SAMPLE) — 모든 분해 칸
                       ② 실적 대조 cycle_year>2025(ACTUALS_LAST_YEAR=2025) — 가드레일에만, 주지표는 안 봄
                       ③ 예산삭감연도 포함 여부 다름 — cohort_compare 단서로만, 판정을 막지 않음
                       ★ 이 도메인엔 A/B 실험이 없어 통신사 예시의 "배정이 깨졌다"(SRM)는 해당 사항 없음
cohort_compare 임계값   MOVE_THRESHOLD 6%p(주지표) · GUARDRAIL_MOVE 2%p(가드레일)
validate.run_checks()  4개 규칙 전부 "ok" — warn 은 0건이다 (Day4에서 이미 확인) ★
                       다만 "날짜 범위" 규칙(ok)의 detail 에 "날짜 결측 38행 ... 리포트 7장 한계로 옮기십시오"가 박혀 있다
가드레일 커버리지      확정배포 442건 중 plan_actuals 347건만 존재(95건은 전부 2026년, 실적 대조 기간 미도래) — 결측이 아니라 의도된 설계
```

★ 표시 둘이 오늘을 관통한다 — **주지표는 실험이 없어 "비교가 깨진 것" 갈래가 통째로 빠진다**는 점과, **warn이 0건이어도 detail 안에 결측 안내가 숨어 있다**는 점이다. 교안 프롬프트를 그대로 쓰면 이 둘을 놓친다.

---

## 먼저 — 페이지1 「한 장 요약」예시

**원본**
```
✕ "모바일 완료율이 낮다"
○ "모바일 35.9% (13,472명 중 4,835명) vs 데스크톱 55.2%.
   격차 19.3%p. 모바일이 이 구간 진입자의 70.4%"
```

**치환**
```
✕ "이사회 승인이 어렵다"
○ "경영진검토→이사회승인 전환율 87.2%(538건 중 469건) — 5단계 중 최대 병목.
   부서별로 쪼개면 최저~최고 약 19%p 격차"
```

구체적인 부서명·건수·비중은 지어내지 않는다. 실습 A~B에서 실제로 조회해 채운다.

---

## 2부. 이 도메인에서는 이렇게 갈립니다 — 참조

교안의 통신사 예시 4행(누적전환율/모바일완료율/절차단축실험/만족도)은 "실험이 깨졌다"·"응답률이 낮다"는 이 도메인에 없는 갈래다. `trust_check()`가 실제로 막는 조건 두 가지로 바꾼다.

| 숫자 | 근거로 | 왜 |
|---|---|---|
| 전체 통과율 74.92% | 쓸 수 있다 | 442/590. 분모가 있다 |
| 경영진검토→이사회승인 부서별 전환율 | 쓸 수 있다 | `funnel_by(department_name)`. 비교(최저~최고)·비중 있음 |
| 2026 코호트 계획-실적 괴리율 | 못 쓴다 | 못 믿을 조건 ② `cycle_year>2025` — `plan_actuals` 자체가 존재하지 않는다(347/442만) |
| 부서×연도 칸 평균 (예: 표본 30건 미만 칸) | 못 쓴다 | 못 믿을 조건 ① 칸 표본 30건 미만 |

셋째 줄이 통신사 예시의 "절차단축실험"(계산은 됐는데 안 보여준다) 자리를 대신한다 — 이 도메인은 계산 자체를 안 한다(`_guardrail_mean()`을 조건 통과 후에만 부른다). **감춰지는 것이 아니라 계산되지 않는 것**이라는 점이 다르다.

---

## 3부. 함께 만듭니다

### 실습 A — 앱에서 숫자를 꺼냅니다

#### 프롬프트 1 — 지표 전수 · 작업형 · 5분

**원본**
```
내 앱의 core/metrics.py 를 써서 아래를 전부 조회하고 표로 보여줘.

- kpis() 가 돌려주는 지표 전부 — 이름 · 값 · 단위
- 각 지표의 분자와 분모가 무엇인지도 함께
- monthly() 로 월별 추이 (마지막 3개월은 따로 표시)

★ 화면에 뜨는 값이 아니라 함수를 직접 호출해서 가져와줘.
★ 반올림하지 말고 소수 둘째 자리까지.
★ 분모를 못 찾는 지표가 있으면 "분모 불명" 이라고 적어줘.
```

**치환**
```
core/metrics.py 의 kpis() 를 직접 호출해서 아래를 표로 보여줘.

- kpis() 가 돌려주는 지표 전부 — 이름 · 값 · 단위 (3개뿐이어야 한다. 4개면 뭔가
  잘못 추가된 것이다 — 재도전율은 임계값이 없어 일부러 뺀 지표다)
- 각 지표의 분자와 분모가 무엇인지도 함께
- monthly() 로 월별 추이 — 2022~2026년 1~3월만 나온다(4~12월은 제출 0건이라
  행 자체가 없다). 없는 것을 결측으로 착각하지 마.

★ 화면에 뜨는 값이 아니라 함수를 직접 호출해서 가져와줘.
★ 반올림하지 말고 소수 둘째 자리까지.
★ 분모를 못 찾는 지표가 있으면 "분모 불명" 이라고 적어줘.
```

**확인할 것** — 카드가 정확히 3개인가 · 계획-실적 괴리율이 평균 집계인지(중앙값 아님) · monthly() 15행(5개년 × 1~3월)이 나오는가.

---

#### 프롬프트 2 — 퍼널과 이탈 · 작업형 · 5분

**원본**
```
funnel() 을 돌려서 단계별로 보여줘.

 단계 | 도달 | 단계 전환율 | 누적 전환율 | 이탈 인원

그리고 아래도 알려줘.
- 단계 전환율이 가장 낮은 구간 (병목)
- 이탈 인원이 가장 많은 구간
- 둘이 같은 구간인가 다른 구간인가 ★

★ 셋째가 다르면 그 이유도 한 줄로.
```

**치환**
```
metrics.funnel() 을 돌려서 5단계(부서초안제출·예산실1차조정·경영진검토·이사회승인·
확정배포) 를 표로 보여줘.

 단계 | 도달 | 단계 전환율 | 누적 전환율 | 이탈(drop) 인원

그리고 아래도 알려줘.
- 단계 전환율이 가장 낮은 구간 (병목) — 87.2%(경영진검토→이사회승인)가 맞는지 확인
- 이탈 인원이 가장 많은 구간
- 둘이 같은 구간인가 다른 구간인가 ★

★ 셋째가 다르면 그 이유도 한 줄로.
★ 1단계의 step_rate 와 drop 은 결측(NaN)이어야 한다 — 0이 아니다. 0으로 나오면
  버그다 (config.FUNNEL_STEPS 이름이 안 맞을 때 ValueError 가 나야 하는 자리다).
```

이 도메인은 실험이 없어 부록 A(퍼널 없는 도메인)는 해당 사항 없음 — 넘어간다.

**확인할 것** — 병목 구간과 이탈 최다 구간이 실제로 같은지 다른지 · 1단계 값이 결측(NaN)으로 나오는지.

---

### 실습 B — 근거의 넷을 채웁니다 ★ 오늘의 장면

#### 프롬프트 3 — 분모 · 비교 · 비중 · 작업형 · 6분

**원본**
```
funnel_by() 를 써서 관심 있는 구간을 축별로 쪼개줘.

 구간: [ 어느 단계 -> 어느 단계 ]
 축:  내 데이터에서 쪼갤 수 있는 것 전부 (한 번에 하나씩)

각 축마다 이 형태로 보여줘.

 칸 이름 | 도달 | 전환 | 전환율 | 비중 | 최고-최저 격차

★ 비중이 무엇에 대한 비중인지 명시해줘 —
  전체 트래픽인가, 그 구간 도달자 중인가.
★ 격차가 가장 큰 축과 가장 작은 축을 둘 다 알려줘.
```

**치환**
```
metrics.funnel_by() 를 써서 병목 구간을 축별로 쪼개줘.

 구간: 경영진검토 -> 이사회승인
 축:  department_name(부서) · division_type(사업부/지원부서) · cycle_year(예산삭감연도 여부)

각 축마다 이 형태로 보여줘.

 칸 이름 | 도달 | 전환 | 전환율 | 비중 | 최고-최저 격차

★ 비중이 무엇에 대한 비중인지 명시해줘 —
  전체 590건에 대한 비중인가, 경영진검토 도달자(538건) 중 비중인가.
★ 격차가 가장 큰 축과 가장 작은 축을 둘 다 알려줘.
★ division_type 은 격차가 0.2%p 로 작을 것으로 예상된다 — 그래도 표에 넣어줘.
  "이 축으로는 갈리지 않는다"도 오늘 결과다.
```

**확인할 것** — 첫 줄(비중의 분모)을 명시했는가 · department_name 격차가 약 19%p 근처로 나오는가 · division_type 이 실제로 0.2%p 근처로 안 갈리는지.

---

#### 프롬프트 4 — 표본을 흔들어 봅니다 · 작업형 · 5분

**원본**
```
방금 나온 각 칸에 대해,
한 건이 바뀌면 전환율이 얼마나 흔들리는지 계산해줘.

 칸 | 분모 | 한 건이 바뀔 때 흔들리는 폭(%p)

그리고 흔들림 폭이 칸 사이 격차보다 큰 축이 있으면 크게 표시해줘.
★ 그 축은 결론을 낼 수 없는 축이다.
```

**치환**
```
프롬프트 3에서 나온 각 칸에 대해,
한 건이 바뀌면 전환율이 얼마나 흔들리는지 계산해줘.

 칸 | 분모 | 한 건이 바뀔 때 흔들리는 폭(%p)

그리고 흔들림 폭이 칸 사이 격차보다 큰 축이 있으면 크게 표시해줘.
★ 그 축은 결론을 낼 수 없는 축이다.
★ 분모가 30건(config.MIN_CELL_SAMPLE) 미만인 칸이 있으면 이름만 따로 적어줘 —
  이 프로젝트에서는 "표본이 얇다"는 판정 자체가 못 믿을 조건 ①로 코드에 이미
  들어가 있다(trust_check). 이 프롬프트로 그 경계값이 실제로 맞는지 확인하는 셈이다.
```

**확인할 것** — 흔들림 폭이 격차보다 큰 칸이 있는가 · 그 칸이 30건 미만 칸과 겹치는가(대개 겹친다).

---

### 실습 C — 정의가 지금도 맞는지 봅니다

#### 프롬프트 5 — 그레인 · 분모 · 기간 · 작업형 · 5분

**원본**
```
방금 조회한 값들에 대해 아래를 확인해줘.

1. 그레인 — 무엇을 하나로 세고 있는가.
   같은 대상이 두 번 세어질 수 있는 구조인가
2. 분모 — 각 비율의 분모가 정확히 무엇인가
3. 기간 — 어느 기간이 들어갔는가.
   아직 진행 중이라 결과가 안 나온 건이 섞여 있는가

★ CLAUDE.md 에 적어 둔 정의와 지금 코드가 하는 일이
  다른 곳이 있으면 알려줘.
```

**치환**
```
방금 조회한 값들에 대해 아래를 확인해줘.

1. 그레인 — plan_id 로 고유하게 세는 계획안 1건이 맞는가.
   plan_stage_events 는 그레인이 다르다(계획안이 특정 단계에서 겪은 시도 1회) —
   지금 조회한 값이 이 둘을 섞고 있지 않은가
2. 분모 — 퍼널 cum_rate 의 분모가 plans 전체 590건인지, 이벤트 로그 1단계
   도달 수인지 (지금은 둘 다 590으로 같지만, 다른 값이면 알려줘)
3. 기간 — config.PERIOD(2022-01-01~2026-08-31) 밖의 건이 섞여 있는가

★ CLAUDE.md 에 적어 둔 정의와 지금 코드(core/metrics.py, core/config.py)가
  하는 일이 다른 곳이 있으면 알려줘.
```

**확인할 것** — CLAUDE.md 와 코드가 어긋나는 곳이 실제로 없는지(있으면 오늘의 발견이다).

---

#### 프롬프트 6 — 유효 구간 · 작업형 · 4분

**원본**
```
가장 최근 구간의 값이 앞 구간보다 낮게 나오는 지표가 있는지 봐줘.

있으면 아래를 확인해줘.
- 그 구간이 아직 안 찬 것인가 (결과가 나올 시간이 안 지났다)
- 8주차에 정한 유효 구간이 있으면 그것을 적용한 값도 함께

★ 안 찬 구간을 포함한 값과 제외한 값을 나란히 보여줘.
  어느 쪽을 쓸지는 내가 정한다.
```

**치환**
```
2026 코호트(제출월 기준)의 값이 앞 연도보다 낮게 나오는지 봐줘.

있으면 아래를 확인해줘.
- plan_actuals 가 존재하지 않아서(095건은 전부 2026년) 계획-실적 괴리율이
  결측으로 나오는 것인지, 실제로 나빠진 것인지
- 전체 통과율(주지표)은 2026 코호트도 포함해서 본다 — 성숙 기준(165일, p90,
  제출→확정배포)을 이미 넘긴 표본이 대부분이기 때문이다
- 계획-실적 괴리율(가드레일)은 2025까지만 본다 — ACTUALS_LAST_YEAR=2025

★ 2026 포함 값과 2025까지만 본 값을 나란히 보여줘.
  어느 쪽을 쓸지는 내가 정한다. 주지표와 가드레일의 유효구간이 다르다는 것
  자체가 이 도메인의 결정이다(하나로 통일하지 않는다).
```

**확인할 것** — 2026 코호트에서 계획-실적 괴리율만 결측이고 전체 통과율은 값이 있는지(그래야 정상).

---

### 실습 D — 못 쓰는 숫자를 가립니다

#### 프롬프트 7 — 감춘 항목 · 작업형 · 5분

**원본**
```
8주차에 못 믿을 조건으로 감춰진 항목을 전부 보여줘.

 무엇이 | 어느 조건에 걸려서 | 지금 몇 건인가 | 무엇이 채워지면 판정 가능한가

★ 감춰진 값 자체는 적지 마. 조건 값과 건수만.
★ 지금 내가 근거로 쓰려는 숫자 중에
  감춘 항목에서 나온 것이 있으면 표시해줘.
```

**치환**
```
metrics.cohort_compare() 와 trust_check() 를 써서, 못 믿을 조건 ①·②에 걸려
계산 자체가 안 된(판정하지 않은) 코호트·칸을 전부 보여줘.

 무엇이 | 조건(① 칸 표본 30건 미만 · ② 실적대조 cycle_year>2025) | 지금 몇 건인가 | 무엇이 채워지면 판정 가능한가

★ 감춰진 값(%p) 자체는 적지 마. 조건 값과 건수만.
★ 지금 내가 근거로 쓰려는 숫자 중에 30건 미만 칸이나 2026 코호트에서 나온
  것이 있으면 표시해줘.
★ 조건 ③(제도가 다름 — 예산삭감연도 포함 여부)은 판정을 막지 않는 단서일 뿐이니
  여기 표에는 넣지 마.
```

이 도메인엔 A/B 실험이 없어 "비교가 깨진 것"(SRM, 배정 붕괴) 갈래는 애초에 해당 사항이 없다.

**확인할 것** — 표에 조건 ①·②만 있고 ③이 안 섞였는가 · 못 쓴다고 나온 항목 중 실제로 내가 발견 문장에 쓰려던 값이 있었는지.

---

#### 프롬프트 8 — 검증 경고와 편향 · 작업형 · 4분

**원본**
```
validate 의 검증 결과를 다시 보여주고,
경고(warn)로 잡힌 항목이 지금 내 숫자에 영향을 주는지 판정해줘.

 경고 항목 | 무슨 내용 | 내 근거에 영향 있나 | 어떻게 다뤄야 하나

★ 응답률이 낮은 지표, 결측이 많은 컬럼,
  특정 집단만 남은 지표가 있으면 특히 짚어줘.
★ "영향 없음" 으로 넘기지 말고 왜 없는지 한 줄로.
```

**치환**
```
validate.run_checks() 의 검증 결과를 다시 보여줘.

★ 지금은 4개 규칙이 전부 "ok"라 warn 은 0건이다. 그렇다고 "확인할 것이 없다"로
  넘기지 마 — 각 규칙의 detail 텍스트를 전부 펼쳐서 그 안에 결측·제외 안내가
  숨어 있는지 확인해줘. 특히 "날짜 범위" 규칙(레벨 ok) 안에 날짜 결측 38행
  안내가 있을 것이다.

 규칙 | 레벨 | detail 안에 내가 놓칠 뻔한 내용이 있나 | 내 근거에 영향 있나

그리고 plan_actuals 커버리지(확정배포 442건 중 347건만 존재)도 같은 방식으로
판정해줘 — 이건 응답률이 낮은 게 아니라 나머지 95건이 전부 2026년이라 실적
대조 기간 자체가 아직 안 지난, 의도된 결측이다. "결측"과 "아직 존재하지
않음"을 구분해서 적어줘.

★ "영향 없음" 으로 넘기지 말고 왜 없는지 한 줄로.
```

이 도메인엔 설문 응답률 개념이 없다(응답 기반 지표가 아니다) — 교안의 "응답률이 낮은 지표" 항목은 plan_actuals 커버리지로 대체한다.

**확인할 것** — warn 0건인데도 detail 안의 날짜 결측 38행을 찾아냈는가 · plan_actuals 95건 결측을 "안 뺐으면 값이 부풀어 있다"가 아니라 "대조 기간이 아직 안 지났다"로 정확히 구분했는가.

---

### 실습 E — 발견을 확정합니다 ★

#### 프롬프트 9 — 발견 문장 후보 · 작업형 · 6분

**원본**
```
지금까지 조회한 것 중 근거 넷이 다 갖춰진 것만 골라
아래 형태의 문장으로 만들어줘.

 [ 무엇 ] 이 [ 값 ] ( [ 분자 ] / [ 분모 ] ) 이다.
 [ 비교 대상 ] 은 [ 값 ] 이다. 격차 [ 얼마 ].
 이것이 [ 무엇 ] 의 [ 비중 ] 을 차지한다.

★ 넷 중 하나라도 빠지면 만들지 말고 무엇이 없는지 알려줘.
★ 원인을 쓰지 마. "때문에" 가 들어가면 그건 오늘 것이 아니다.
★ 5개를 넘기지 마.
```

**치환**
```
지금까지 조회한 것 중 근거 넷이 다 갖춰진 것만 골라
아래 형태의 문장으로 만들어줘.

 [ 무엇 ] 이 [ 값 ] ( [ 분자 ] / [ 분모 ] ) 이다.
 [ 비교 대상 ] 은 [ 값 ] 이다. 격차 [ 얼마 ].
 이것이 [ 무엇 ] 의 [ 비중 ] 을 차지한다.

★ 넷 중 하나라도 빠지면 만들지 말고 무엇이 없는지 알려줘.
★ 원인을 쓰지 마. "때문에"·"까다로워서" 가 들어가면 그건 오늘 것이 아니다.
★ 5개를 넘기지 마.
★ 못 믿을 조건 ①·②에 걸린 칸·코호트에서 나온 숫자는 후보에서 아예 빼줘.
```

```
✕ "경영진 검토가 까다로워서 이사회 승인 전환율이 낮다"       <- 원인을 단정했다
○ "경영진검토→이사회승인 전환율이 87.2%로 5단계 중 가장 낮다.
   부서별로는 최저~최고 약 19%p 격차가 난다"
```

**확인할 것** — 문장에 "때문에" 류 표현이 없는가 · 5개 이하인가.

---

#### 프롬프트 10 — 크기를 매깁니다 · 작업형 · 5분

**원본**
```
발견마다 크기를 계산해줘.

 발견 | 격차 | 비중 | 격차 x 비중 | 절대 건수로 환산하면

★ 순서는 매기지 마. 표만 보여줘.
★ 격차가 큰데 비중이 작은 것, 비중이 큰데 격차가 작은 것을
  각각 따로 표시해줘.
```

**치환** — 그대로 쓴다. 함수 호출이 없는, 순수 정리형 프롬프트라 도메인 치환이 필요 없다.

**확인할 것** — 격차 × 비중이 뚜렷하게 큰 발견이 하나로 좁혀지는가(대개 경영진검토→이사회승인 병목·department_name 축).

---

### 실습 F — 발견을 기록합니다

#### 프롬프트 11 — 발견 파일 · 작업형 · 4분

**원본**
```
아래 내용을 my-report/발견.md 에 저장해줘.

# 발견
조회 일시: <실행 시각>

## 발견 1 — [ 한 줄 ]
| 값 | ( 분자 / 분모 ) |
| 비교 대상 | |
| 격차 | |
| 비중 | | ( 무엇에 대한 비중인지 명시 )
| 표본 | 한 건 흔들림 폭 |
| 크기 | 격차 x 비중 |

## 못 쓰는 숫자
| 무엇 | 왜 못 쓰나 | 무엇이 채워지면 쓸 수 있나 |

## 확인한 정의
| 그레인 | 분모 | 기간 | 유효 구간 |

★ 조회하지 않은 값을 넣지 마.
★ 원인이나 제안을 쓰지 마. 오늘 것이 아니다.
```

**치환**
```
아래 내용을 발견.md 에 저장해줘 (프로젝트 루트 — my-report/ 폴더는 없다).

# 발견
조회 일시: <실행 시각>

## 발견 1 — [ 한 줄 ]
| 값 | ( 분자 / 분모 ) |
| 비교 대상 | |
| 격차 | |
| 비중 | | ( 전체 590건 기준인지, 그 구간 도달자 기준인지 명시 )
| 표본 | 한 건 흔들림 폭(%p) · 30건(MIN_CELL_SAMPLE) 이상인가 |
| 크기 | 격차 x 비중 |

## 못 쓰는 숫자
| 무엇 | 왜 못 쓰나(못 믿을 조건 ①·② 중 무엇) | 무엇이 채워지면 쓸 수 있나 |

## 확인한 정의
| 그레인 | 분모 | 기간 | 유효 구간 |

★ 조회하지 않은 값을 넣지 마.
★ 원인이나 제안을 쓰지 마. 오늘 것이 아니다.
```

**확인할 것** — 파일이 프로젝트 루트에 생겼는가(`판단기준.md`와 같은 위치) · 「못 쓰는 숫자」 절이 비어 있지 않은가.

---

## 판단 기준 한 장

### 프롬프트 12 — 판단 기준 한 장 · 작업형 · 3분

**원본**
```
오늘 배운 것 중 도메인과 무관한 문장만 남겨 한 장으로 정리해줘.

- 특정 업종·데이터 이름이 남아 있으면 빼줘
- 내가 오늘 내린 결정도 한 줄씩
  (어느 숫자를 버렸고 왜, 크기를 무엇으로 쟀는지)
- my-report/판단기준.md 에 날짜와 함께 덧붙여줘. 덮어쓰지 마
```

**치환**
```
오늘 배운 것 중 도메인과 무관한 문장만 남겨 한 장으로 정리해줘.

- 특정 업종·데이터 이름이 남아 있으면 빼줘
  (경영계획·부서·이사회 승인·예산삭감연도 같은 단어가 남으면 안 된다)
- 내가 오늘 내린 결정도 한 줄씩
  · 어느 숫자를 버렸고 왜 — 못 믿을 조건 ①·② 중 무엇에 걸렸는지
  · 크기를 무엇으로 쟀는지 — 격차 × 비중
  · 어느 축을 오늘 근거로 썼는지 — department_name(판정축)
- 판단기준.md 에 날짜(2026-09-08)와 함께 덧붙여줘. 덮어쓰지 마

그리고 지금까지의 회차를 다 모아서 중복을 정리해줘.
같은 말이 두 번 나오면 하나로 합쳐줘.
```

이 항목도 Day4와 같은 이유로 매 회차 반복 실행된다 — `판단기준.md`에 이미 09-01~09-04(4회차)가 쌓여 있으므로 오늘은 다섯 번째 회차를 더하고 통합한다.

# 판단기준

도메인이 바뀌어도 남는 문장만 모은다. 업종·테이블·컬럼 이름이 보이면 그건 예시라 뺀다.
회차마다 **덧붙인다.** 지우지 않는다 — 나중에 판단이 바뀌면 바뀐 이유까지 남아야 한다.

---

## 2026-09-01

### 차단과 경고를 가르는 질문

**이 규칙이 깨진 채로 계산하면 값이 틀리는가?**
틀린다 → 차단. 해석만 조심하면 된다 → 경고.

차단을 늘리면 안전해 보이지만 실무 데이터는 늘 어딘가 깨져 있어서 아무것도 못 돌리게 된다.
차단은 **"이대로 계산하면 확실히 틀리는 것"에만** 건다.

### 오늘 내린 결정

**차단으로 정한 것**

- **필수 컬럼이 없으면 차단** — 계산이 멈추거나, 그 컬럼을 쓰는 지표가 조용히 빠진다. 해석으로 보정할 수 있는 종류가 아니다.
- **분석 기간을 벗어난 행이 있으면 차단** — 표지에 적은 기간과 실제 집계 대상이 달라진다. 숫자가 선언한 기간과 안 맞으므로 해석으로 못 고친다.
- **최소 표본 미달이면 차단** — 그보다 적으면 지표를 계산하지 않기로 **미리 정했다.** 계산해도 쓸 수 없는 값이 나온다.

**경고로 정한 것**

- **행 수가 기준 대비 ±10% 벗어나면 경고** — 데이터가 갱신되면 늘어나는 것이 정상이라 차단할 근거가 없다. 다만 조용히 절반이 사라지는 것은 눈치채야 한다.

**규칙으로 만들지 않고 드러내기만 한 것**

- **날짜 결측은 차단도 경고도 걸지 않고, 건수를 검증 상세에 적어 리포트 한계로 넘겼다** — 결측을 다른 값으로 대체할 근거가 없다. 근거가 없을 때는 감추지도, 차단하지도 않고 **보이게 둔다.**

**임계값을 정할 때**

- 근거가 실측인 값과 **임의로 정한 값을 구분해 적었다.** 임의 지정은 "임의 지정, 통계적 산출 근거 없음"이라고 그대로 남겼다. 근거 없는 임계값은 남의 것이다.
- 가드레일처럼 **높을수록 나쁜 지표**는 판정 방향을 뒤집어 등록해야 한다. 안 하면 좋은 값이 위험으로 뜬다.
- 최소 표본은 **집계 단위마다 다르다.** 전체로는 충분한 표본이 하위 조합으로 쪼개면 미달할 수 있고, 그 단위 보고는 노이즈를 신호로 오인하게 만든다.

### 검증은 깨뜨려 봐야 존재한다

- 검증을 만든 뒤 **정상 데이터를 일부러 망가뜨려** 차단이 뜨고 진행 버튼이 비활성되는 것까지 눈으로 봐야 한다. **경고만 띄우고 진행되면 그 검증은 없는 것과 같다.**
- 돌리기 **전에 어떤 판정이 나와야 하는지 먼저 말한다.** 그러지 않으면 나온 결과에 맞춰 해석하게 된다. 예상 건수까지 적어 두면 대조가 된다.
- **"통과"가 떴을 때 먼저 볼 것은 무엇을 검증했는지다.** 망가뜨린 데이터가 검증에 도달하지 못했을 수 있다. 행 수·컬럼 수 같은 표면 숫자로 "지금 본 것이 그것인지" 확인한다.
- 원본을 쓰는 실험은 **지우지 말고 옮긴다.** 끝나면 체크섬으로 원복을 확인한다.
- 명세에 적어둔 수치는 **실제로 다시 계산해서** 맞는지 본다. 옮겨 적는 과정에서 틀리는 일이 실제로 생긴다.

### 값과 표시를 분리한다

- 계산이 필터로 쓰는 문자열과 사람에게 보여주는 이름은 **다른 곳에 둔다.** 표시용 이름을 계산 쪽에 넣으면 결과가 조용히 0이 된다.
- 원천 데이터와 **집계 결과·스냅샷을 섞지 않는다.** 이미 요약된 파일은 원천이 아니고, 그것으로는 개별 단위 추적이 안 된다.
- 가정값과 실측값을 섞지 않는다. 가정값이 들어간 문장에는 가정값 기반임을 붙인다.

### 기록은 저장 시점이 있어야 남는다

- 판단한 시점과 **기록이 파일로 떨어지는 시점은 다르다.** 저장 호출이 없으면 판단 기록은 메모리에만 있다가 사라진다. 거버넌스 기록이라고 부르려면 **어디서 저장되는지**를 확인해야 한다.
- 판단 근거를 비워도 통과되는 구조라면, 그 기록은 "누가 언제"만 남고 "무엇을 보고"가 빈다.

### 도구가 만드는 착각 (프레임워크 사실)

- **서버는 기동 시점의 코드를 들고 있다.** 화면에 옛 값이 보이면 코드가 아니라 프로세스를 의심한다. 파일 수정 시각과 프로세스 시작 시각을 비교하면 바로 나온다.
- **캐시된 함수는 입력 파일이 바뀌어도, 설정값이 바뀌어도 다시 계산하지 않는다.** 캐시 키는 보통 **인자뿐**이라, 함수가 참조하는 설정을 고쳐도 무효화되지 않고 옛 결과가 돌아온다. 데이터를 바꿔 시험할 때도, 기준값·단계 정의를 고친 뒤에도 캐시를 먼저 비운다. 코드가 다시 실행되는 것과 캐시가 비워지는 것은 별개다.
  - 그래서 **설정을 고친 뒤 방어 코드가 도는지 시험하면 통과한 것처럼 보인다.** 예외가 안 났다면 방어가 없는 것인지 캐시가 남은 것인지 먼저 가른다.
- **함수 인자는 함수 호출 전에 평가된다.** 그래서 인자 자리에서 터진 예외는 그 함수의 예외 처리로 잡히지 않는다.
- **실행을 중단시키는 호출 아래는 그려지지 않는다.** 로그를 화면 맨 끝에만 두면, 중간에 멈춘 순간 왜 멈췄는지가 같이 사라진다. 멈추기 전에 그린다.
- 화면이 열리는 것(HTTP 200)은 동작 확인이 아니다. 버튼을 누르고 판단을 통과시키고 결과물을 만들어 봐야 안다.

---

## 2026-09-02

### 오늘 내린 결정

- **그레인을 「대상 1건」으로 정하고 고유값으로 셌다** — 한 대상이 같은 단계를 두 번 밟은 기록이 있어서, 행을 그대로 세면 재시도한 대상이 여러 번 세어진다. 세는 단위를 정하지 않고 시작하면 나머지 계산이 전부 그 위에 쌓인다.
- **유지를 「되돌릴 수 없는 종료」의 여집합으로 정의했다** — 종료가 확정된 것만 이탈로 세고, **아직 결론이 안 난 진행 중 상태는 이탈로 보지 않는다.** 진행 중을 이탈에 넣으면 시간이 지나면 뒤집힐 판정을 지금 내리는 셈이다.
- **이탈을 유지 퍼널의 단계로 넣지 않았다** — 유지의 여집합이라 앞 단계를 거친 부분집합이 아니다. 단계로 넣으면 뒤 단계 수가 다시 늘어 전환율 해석이 안 된다. 여집합은 단계가 아니라 나머지로 읽는다.
- **가드레일 지표를 중앙값이 아니라 평균으로 잡았다** — 기준선이 평균에서 나왔고, 분포가 한쪽으로 치우쳐 있어 잡아야 하는 것이 그 꼬리다. 놓치는 쪽이 위험한 지표는 민감한 편을 고른다.
- **기간을 전체 한 덩어리로 두고, 쪼개는 것은 다음 회차로 미뤘다** — 반환 형식이 기간 축을 담지 못해서다. **미룬 이유와 미룬 사실을 코드 주석에 적었다.** 안 적으면 나중에 그 자리가 왜 그런지 알 수 없다.
- **근거 없는 임계값은 만들지 않았고, 대신 판정하지 않았음을 색으로 드러냈다** — 임계값이 없는 지표가 초록 "정상"으로 뜨는 것을 회색 "판정 없음"으로 바꿨다. 감추는 것보다 나쁜 것은 **판정한 적 없는 것을 통과했다고 주장하는 화면**이다.
- **규칙이 현실과 어긋나면 예외 목록을 만들지 않고 규칙을 다시 썼다** — 「정의는 한 파일에서만」이 이미 여러 곳에서 깨져 있었다. 값(기준·순서·이름)과 계산 방법(판정식·필터)을 갈라 쓰니 어긋남이 없어졌다.
- **합성 데이터는 저장소에 넣기로 했다** — 공개 정보가 아니고 받은 사람이 바로 앱을 띄울 수 있다. **다만 실측 데이터로 바꿀 때 이 결정을 되돌려야 한다는 사실을 제외 규칙 주석으로 남겼다.**

### ★ 도구가 말 안 하고 정한 것 — 무엇을 잡아냈나

**함수를 채우게 하면 지시에 없는 빈칸을 반드시 스스로 메운다.** 그것 자체는 피할 수 없고, **무엇을 임의로 정했는지 목록으로 요구하는 것**이 검토 방법이다. "네가 임의로 정한 것을 전부 말해라 · 왜 그렇게 골랐나 · 다르게 고를 수도 있었던 것"으로 물으면 나온다. 오늘 그렇게 해서 잡은 것들이다.

- **데이터에 없는 값을 조용히 0으로 세는 처리** — 이름이 어긋나면 그 항목이 0이 된다. **"정말 아무도 없었다"와 "이름이 틀렸다"가 구분되지 않아**, 틀린 결과가 정상처럼 보인다. 가장 위험한 종류였다.
- **같은 표 안에서 첫 행 처리가 일관되지 않은 것** — 한 열은 결측으로, 다른 열은 0으로 두고 있었다. 0은 "없었다"로 읽히는데 실제로는 "해당 없음"이다. 읽는 사람이 헷갈린다.
- **판정 기준이 없는 항목을 "정상"으로 판정한 것** — 어느 지표는 그 이유로 빼면서 다른 지표에는 같은 결함을 남겨 두었다. **일관되지 않은 채로 넘어갔고, 물어봐야 드러났다.**
- **같은 목록이 두 곳에 중복으로 있던 것** — 한쪽에 항목이 빠져 있어 같은 값을 한 화면은 개선으로, 다른 화면은 악화로 판정하고 있었다.
- **프롬프트에 적어 준 근거가 실제로는 성립하지 않았던 것** — "중복이 있으니 고유값으로 세라"고 지시했는데, 앞선 필터가 이미 그 일을 하고 있어 두 방식의 결과가 같았다. 지시가 맞는 말이어도 **그 이유가 맞는지는 따로 확인해야 한다.**
- **검사가 구조적으로 0만 낼 수 있는데 "검증됐다"로 읽으려 한 것** — 조건이 좁아지기만 하는 항목끼리 포함 관계를 검사하면 위반이 불가능하다. **0이 나왔을 때 물을 것은 "그 검사가 무엇을 검출할 수 있었나"다.**

### 검토를 요구하는 방식

- **"임의로 정한 것을 전부 말해라"** — 지시에 없던 빈칸이 드러난다.
- **"코드 말고 말로 세 줄로 설명해라"** — 설명이 안 되면 그 코드는 이해된 것이 아니다.
- **"이 검사가 무엇을 검출할 수 있었나"** — 통과가 통과인지 가려낸다.
- **"돌리기 전에 어떤 결과가 나와야 하는지 먼저 말해라"** — 나온 값에 맞춰 해석하는 것을 막는다.
- 그리고 **답을 고르라고 하기 전에 값을 실제로 조회하게 한다.** 후보를 나열만 하면 판단할 재료가 없다.

### 신뢰 구간은 지표마다 다르다

- **하나의 유효구간을 전 지표에 쓰지 않는다.** 결과가 확정된 구간 · 하류 지표가 존재하는 구간 · 시각 기록이 온전한 구간은 서로 다르다. 셋의 교집합만 쓰면 표본이 줄고 비교 대상이 사라진다.
- **최근 묶음의 값이 낮을 때 성과인지 시간 부족인지는 두 가지로 가른다** — 아직 결론이 안 난 건수, 그리고 완주까지 걸린 시간의 분포. 경과 시간이 그 분포의 상위 구간에 못 미치면 **판정을 보류하고 하한만 말한다.**
- **소요 시간 분포는 이미 끝난 것들로만 만들어진다.** 끝나지 않은 것은 애초에 빠져 있어 필요한 시간을 과소추정한다 — 생존 편향이 여기서 다시 나온다.

### 교안과 대조해 보강한 것

기록이 「오늘 겪은 것」에 치우쳐 원칙이 빠져 있었다. 대조해서 채운다.

**세는 단위를 바꾸면 다른 숫자가 나오고, 둘 다 맞다**

- 같은 데이터라도 무엇을 하나로 세느냐에 따라 값이 달라진다. **틀린 쪽이 있는 것이 아니라 서로 다른 질문에 답한다.** 하나는 「대상이 끝까지 갔는가」, 다른 하나는 「한 번 올렸을 때 통과하는가」다. 오늘 같은 구간이 87%와 63%로 갈렸고 둘 다 맞는 숫자였다.
- 그래서 **고른 것만 적지 말고 기각한 대안과 그것이 답하던 질문도 남긴다.** 결정만 기록하면 나중에 "왜 이 숫자인가"에 답할 수 없다.
- 단위를 고를 때 물을 것: 그 단위가 **다른 표와 이어지는가**(같은 단위여야 조인·비교가 된다), 그리고 **반환 형식이 그 단위를 담을 수 있는가.**

**한 번 정한 것은 파일에 박아둔다**

- 같은 설명을 매번 다시 하지 않게, 정한 것은 **도구가 읽는 파일**에 적는다. 대화에만 있으면 다음 세션에서 사라지고, 사라지면 도구가 다시 임의로 정한다.
- 적을 것은 **정한 값과 그 근거**다. 값만 적으면 나중에 바꿀 때 무엇을 근거로 정했는지 몰라 다시 임의 지정이 된다.
- **안 정한 것은 「미정」으로 적어 둔다.** 비워 두면 빈칸인지 합의된 것인지 구분되지 않고, 도구는 빈칸을 채운다.

**되돌릴 수 있게 해두면 과감하게 시킬 수 있다**

- 되돌릴 수 없으면 조심스러워지고, **조심스러우면 시험하지 않게 되고, 시험하지 않으면 배우지 못한다.** 그래서 되돌림 수단을 먼저 확보하고 나서 과감하게 시킨다.
- 되돌림 수단은 세 가지다 — **작업 단위로 기록을 남기는 것**(무엇이 언제 바뀌었는지), **원본을 지우지 않고 옮기는 것**, **되돌린 뒤 원본과 같은지 확인하는 것**(체크섬).
- **되돌릴 수 없는 것과 있는 것을 구분해 둔다.** 발송·삭제·외부로 나가는 것은 되돌릴 수 없다. 그 경계에서만 조심하고, 나머지는 과감하게 한다.

**분류를 퍼널이라 부르지 않는다**

- **앞 단계를 거치지 않고 다음 단계에 나타난 대상이 많으면 그것은 퍼널이 아니라 분류다.** 분류에 전환율을 내면 분모가 앞 단계가 아니므로 그 비율은 아무것도 뜻하지 않는다.
- 판정은 세 질문이다 — 앞 단계를 반드시 거치는가 · 세는 단위가 무엇인가 · 기간을 어떻게 자르는가.
- **여집합은 단계가 아니다.** 나머지로 읽는다. 단계로 넣으면 뒤 단계 수가 다시 늘어난다.
- 위반 건수가 0으로 나왔을 때 먼저 물을 것: **그 검사가 위반을 낼 수 있는 구조였나.** 조건이 좁아지기만 하는 항목끼리는 위반이 불가능하다.

**숫자가 안 맞을 때 의심하는 순서 — 그레인 → 분모 → 필터 → 기간**

*(아직 안 겪음. 오늘은 실측값과 전부 일치해 이 순서를 쓸 일이 없었다. 다음에 어긋날 때 이 순서로 확인한다.)*

- **그레인** 한 행이 무엇인지. 중복·재시도로 부풀지 않았는지
- **분모** 무엇으로 나눴는지. 전체인지, 앞 단계인지, 자격이 있었던 대상인지
- **필터** 무엇을 빼고 세었는지. 상태값·중복 제거가 의도한 대로인지
- **기간** 어느 구간을 잡았는지. 관측 기간이 다른 것이 섞이지 않았는지

앞쪽이 틀리면 뒤쪽을 아무리 맞춰도 안 맞는다. **순서를 지켜 위에서부터 본다.**

---

## 2026-09-03

### 오늘 내린 결정

- **분해축은 격차가 큰 것이 아니라 손을 쓸 수 있는 것을 골랐다** — 후보를 여러 축으로 쪼개 보면 격차가 아예 없는 축, 격차는 있지만 통제 밖인 축, 격차가 있고 실제로 개입할 수 있는 축이 갈린다. 조직의 **성격**을 가르는 축(가만있어도 정해지는 분류)은 격차가 있어도 판정의 대상으로 쓰지 않았다 — 바꿀 수 없는 것을 두고 "여기가 문제"라고 말해도 실행으로 안 이어진다. 반대로 격차가 아예 없는 축은 판정 대상에서 뺐다 — 쪼개도 아무것도 안 보이면 분해할 이유가 없다. **화면에 후보로는 남기고, 판정에는 쓰지 않는다**로 둘을 구분했다.
- **못 믿는 조건 셋을 정했다 — 표본 · 대조 기간 · 비교 공정성.**
  - **표본**: 쪼갠 칸 하나의 크기가 기준 미달이면 안 믿는다. 전체 표본이 충분해도 쪼개면 얇아질 수 있고, 얇은 칸의 비율은 우연을 신호로 오인하게 만든다.
  - **대조 기간**: 결과를 실측으로 대조할 수 있는 기간이 아직 안 지났으면 안 믿는다. 값이 없는 것이 아니라 **아직 존재하지 않는** 것이라, 0이나 결측으로 채우면 "이미 확인했다"는 거짓 인상을 준다.
  - **비교 공정성**: 두 구간을 나란히 놓고 비교할 때, 그 두 구간이 서로 다른 제도·조건 아래 있었는지를 본다. 조건이 다른 두 구간의 차이는 개입 때문인지 조건 때문인지 갈릴 수 없어, 차이가 있어도 "그래서 좋아졌다"고 말할 수 없다.
  - 셋 중 하나라도 걸리면 **그 값을 구하는 계산 자체를 하지 않는다.** 계산해 놓고 화면에 숨기는 것과, 계산을 안 하는 것은 다르다 — 전자는 어딘가에 값이 존재해 실수로 새어나갈 수 있고, 후자는 존재하지 않아 새어나갈 것이 없다.
- **판정 창(유효구간)을 지표마다 다르게 두었다** — 한 창으로 통일하면 좁은 지표의 한계가 넓은 지표까지 끌어내린다. 결과가 이미 확정된 것을 보는 지표는 최근 구간까지 볼 수 있고, 실측 대조가 필요한 지표는 대조가 끝난 구간까지만 볼 수 있다. **둘을 같은 창에 억지로 맞추면, 넓게 볼 수 있는 지표까지 좁은 지표의 사정에 맞춰 가려진다.** 창이 다르다는 사실 자체를 감추지 않고, "왜 여기까지만 보는가"를 조건으로 명시해 둔다.

### 감춘 화면을 설명하는 방법

- **버튼을 누르기 전까지는 안 보인다.** 판정과 근거를 같은 자리에 늘어놓으면 사람은 판정만 보고 근거를 안 읽는다. 감춰진 값 옆에 "왜 감췄는가"를 별도로 열어보는 자리를 두면, 필요한 사람만 근거까지 본다.
- **그 안에는 판정에 쓴 재료(조건 값)만 놓고, 판정 자체나 최종 수치는 다시 안 놓는다.** 두 자리에 같은 숫자를 겹쳐 적으면 어느 쪽이 원본인지 헷갈리고, 하나를 고치면 다른 하나가 낡은 채로 남는다.
- **"무엇을 하면 믿을 수 있는가"를 반드시 같이 적는다.** 못 믿는다는 판정만 던지면 받는 사람이 할 수 있는 일이 없다. 조건별로 해소 방법이 다르므로(표본이 쌓이면 되는 것 / 시간이 지나야 되는 것 / 비교 대상을 바꿔야 되는 것), 어느 조건에 걸렸는지에 따라 다른 문장을 붙인다.
- **걸린 조건이 여러 개일 수 있을 때는, 겉으로 먼저 보이는 이유가 아니라 더 근본적인 이유를 짚는다.** 표본이 없는 것처럼 보여도 실제로는 "아직 그 표본이 생길 시점이 안 됐다"인 경우, 표본 문제로만 안내하면 받는 사람이 엉뚱한 곳(표본을 늘리는 방법)을 고치려 든다.

### 화면 상태를 주소로 공유하기

- **화면에서 고른 조건을 주소(URL)에 실어 두면, 그 주소를 받은 사람이 같은 화면을 그대로 연다.** 화면 안의 선택 상태와 주소가 같은 값을 가리켜야 하고, 한쪽만 바뀌고 다른 쪽이 안 바뀌면 공유가 깨진다.
- **주소에서 읽은 값이 없거나 후보 목록에 없으면 기본값으로 떨어뜨리고, 절대 오류를 내지 않는다.** 공유된 주소는 시간이 지나면 후보가 바뀌어 더 이상 유효하지 않을 수 있다 — 그래도 화면은 열려야 한다.
- **선택 위젯의 기본값과 주소의 값이 어긋나면, 위젯이 이긴 결과로 주소를 다시 맞춘다.** 사람이 화면에서 고른 것이 진실이고, 주소는 그것을 뒤따라가는 표시일 뿐이다.
- **조각(부분 화면)만 다시 그리는 방식과 주소 갱신을 같이 쓰면 어긋난다.** 주소를 바꾸는 선택 위젯은 화면 전체가 다시 그려지는 자리에 두어야, 방금 고른 값과 화면에 보이는 값과 주소 세 가지가 항상 같은 순간의 것이 된다.
- **주소를 그대로 보여줄 때, 도구가 그 주소를 조립하는 방식을 그대로 믿지 않는다.** 실제로 화면에 뜬 값을 보고 나서야 이상한 중복·꼬임이 있는지 알 수 있었다 — 짐작으로 넘어가지 않고 실제 출력을 봐야 한다.
